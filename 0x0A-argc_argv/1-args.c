#include <stdio.h>
#include "main.h"
/**
* main - a function that prints number of arguments passed to it
* @argc: number of arguments
* @argv: array of listed  commands 
* Return: Always 0 (Success)
*/
int main(int argc _attribute_((unused)) char *argv[])
{
	printf("%d/n" , argc);
	Return (0);
